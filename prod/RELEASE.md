# Custombuild 1.0 release candidate

This directory is the self-contained production candidate. Run commands from `prod/`; the repository root remains the historical MVP/reference line.

Generated machine programs remain validation-only until a named physical machine, tooling, material batch, calibration, simulation, air-cut, reference parts and prototype furniture are approved by an experienced furniture constructor and CNC operator.
